export const APP_VERSION = __APP_VERSION__

// « beta » est un état produit assumé, découplé du numéro SemVer (0.x = instable).
// À retirer ici (et uniquement ici) lors de la sortie de beta.
export const APP_VERSION_LABEL = `${APP_VERSION} — beta`
